---
layout: layouts/base.njk
pageClass: projects
---

<div class="container page-projects">
  {% include "header-blog.njk" %}
  <h1>Projects</h1>

  <p>Some of my favourite personal projects, generally smaller in scope than my client work.<br>
    <br>For my commercial work, please <a href='mailto:hello@sonnet.io'>get&nbsp;in&nbsp;touch</a>!</p>

  <ul class='projects-list'>
    {%- for project in projects.favourites -%}
    
      {%  project.isFavourite %}
        <li class='projects-entry'>
          <article>
            <header>
              <h1>{{project.title}}</h1>
              <time datetime="{{ project.date }}">{{ project.date | dateDisplay }}</time>
            </header>

            {%- if project.heroURL %}{% pic project.heroURL,
              project.title %}
            {% endif %}

            <section class='projects-entry__content'>{{ project.content | mdblock | safe}}</section>
            <section class="projects-entry__actions">
              {% if (project.demoURL) %}
              <a href='{{project.demoURL}}' class='projects-entry__link'>Launch</a>
            {% else %}
              <a href='mailto:hello@sonnet.io' class='projects-entry__link'>Contact</a>
            {% endif %}
            </section>

          </article>
        </li>
      {% endif %}
    {%- endfor -%}
  </ul>
  <hr>
</div>